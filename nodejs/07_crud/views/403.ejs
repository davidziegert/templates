<%- include("./partials/head.ejs"); %> <%- include("./partials/header.ejs"); %> <%- include("./partials/nav.ejs"); %>

<main>
  <section>
    <h1 class="error-title"><%= txt_title %></h1>
    <img class="error-image" src="img/error.png" alt="image loading" loading="lazy" />
    <p>The <strong class="error-status"><%= txt_title %></strong> indicates that the server understood the request but refuses to authorize it. A server that wishes to make public why the request has been forbidden can describe that reason in the response payload (if any).</p>
  </section>
</main>

<%- include("./partials/aside.ejs"); %> <%- include("./partials/footer.ejs"); %>
