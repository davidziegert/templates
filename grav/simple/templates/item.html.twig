{% extends 'partials/base.html.twig' %}

{% block main %}
	<section>
		{% block content %}

			<blockquote>
				<h1>{{ page.title }}</h1>
				{{ page.content|raw }}
			</blockquote>

		{% endblock content %}
	</section>
{% endblock main %}
